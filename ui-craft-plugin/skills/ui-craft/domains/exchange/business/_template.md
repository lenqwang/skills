# Business 子域模板 — 标准化 6 区块结构

> 所有 business 文件**必须**严格遵循以下 6 个区块，顺序固定，不可省略。
> 缺失的区块标注"无"或"同默认"，不可删除区块标题。

---

## 区块 1: 元信息

```yaml
name: <business-id>           # 文件名（不含 .md），如 rewards-hub
description: <一句话描述>
figma: <Figma 设计文件 URL>
coverage: H5 | Web | H5+Web   # 覆盖平台
theme: dark | light | both     # 主题模式
```

---

## 区块 2: 业务画像

- **目标用户**：[用户类型1]、[用户类型2]
- **核心场景**：[场景描述]
- **页面类型**：[页面1]、[页面2]
- **适用标签**：`[标签1]` `[标签2]`

| 维度 | 本业务 | Exchange 默认 | 说明 |
|------|--------|--------------|------|
| 主题 | dark/light | — | |
| 信息密度 | 高/中/低 | 高 | |
| 视觉风格 | 营销/工具/交易 | 交易 | |

---

## 区块 3: Token 覆盖

> 与 Exchange 基础 Token 的差异项。未列出的使用 `../semantic.md` 默认值。

### 3.1 颜色变量

| 变量 | 值 | 说明 |
|------|-----|------|
| `--bg-primary` | #xxx | |

### 3.2 亮色 + 暗色（如适用）

```css
:root { /* 亮色覆盖 */ }
[data-theme="dark"] { /* 暗色覆盖 */ }
```

---

## 区块 4: 页面结构

> 页面组合图。引用 `../components/` 中的共享组件 + 本业务专属组件。

### 页面类型 A

```
┌─────────────────────────────────────────┐
│  [共享组件名]                            │
├─────────────────────────────────────────┤
│  [业务专属组件名]                        │
└─────────────────────────────────────────┘
```

**组件引用**：

| 区域 | 组件 | 来源 |
|------|------|------|
| 倒计时 | CountdownTimer | [共享](../components/countdown-timer.md) |
| 任务行 | TaskRow | 业务专属（见区块 5） |

---

## 区块 5: 业务专属组件

> 仅本业务独有的组件定义。通用组件引用 `../components/` 不在此重复。

### [组件名称]

```
[ASCII 结构图]
```

| 属性 | H5 值 | Web 值 | 说明 |
|------|-------|--------|------|
| 宽度 | 335px | 1200px | |
| 高度 | — | — | |

---

## 区块 6: 适用性 & H5/Web 差异

### 适用场景

- [场景1]
- [场景2]

### 不适用场景

- [场景1]（建议用 xxx）

### H5 vs Web 速查

| 维度 | H5 | Web |
|------|-----|-----|
| 内容区宽度 | 335px | 1200px |
